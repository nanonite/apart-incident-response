# Repository Commit Guidance

- `.chainlink/issues.db` is tracked project state and should be included when it changes.
- `runs/qwen3-8b/` contains intentional experiment artifacts and should be included when the Ollama runner work is committed.
- Do not exclude these paths as generated output unless the user explicitly asks for a source-only commit.
