[project]
name = "apart-incident-response"
version = "0.1.0"
description = "Controlled runtime for the accidental-coordination experiment"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = []

[tool.uv]
package = false
